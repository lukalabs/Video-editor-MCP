{
  "id": "button-ghost-float",
  "name": "Button Ghost Float",
  "description": "Translucent pill that gently floats",
  "project": "src/projects/button-ghost-float.ts",
  "durationParam": "durationInSeconds",
  "defaultWidth": 1080,
  "defaultHeight": 1920,
  "params": [
    {
      "key": "text",
      "label": "Button text",
      "type": "text",
      "default": "Get Started",
      "description": "The label. The button sizes itself to it: longer text makes a wider button, very short text keeps a minimum width, and text too wide for the frame wraps onto more lines."
    },
    {
      "key": "backgroundColor",
      "label": "Tint colour",
      "type": "color",
      "default": "#ffffff",
      "description": "Colour of the hairline border and of the translucent fill."
    },
    {
      "key": "textColor",
      "label": "Text colour",
      "type": "color",
      "default": "#ffffff",
      "description": "Colour of the label."
    },
    {
      "key": "fillOpacity",
      "label": "Fill opacity",
      "type": "number",
      "default": 0.18,
      "min": 0,
      "max": 0.9,
      "step": 0.01,
      "description": "How opaque the glassy fill is; 0 is border only."
    },
    {
      "key": "floatAmount",
      "label": "Float distance (px)",
      "type": "number",
      "default": 12,
      "min": 0,
      "max": 60,
      "step": 1,
      "description": "How far the button drifts up and down while idle."
    },
    {
      "key": "fontSize",
      "label": "Font size (px)",
      "type": "number",
      "default": 64,
      "min": 16,
      "max": 200,
      "step": 1,
      "description": "Label size. The button's height follows it, and its width follows the measured text."
    },
    {
      "key": "positionY",
      "label": "Vertical position (0 top / 1 bottom)",
      "type": "number",
      "default": 0.5,
      "min": 0.05,
      "max": 0.95,
      "step": 0.01,
      "description": "Where the button's centre sits in the frame, 0 at the top edge and 1 at the bottom."
    },
    {
      "key": "durationInSeconds",
      "label": "Duration (s)",
      "type": "number",
      "default": 4,
      "min": 0.5,
      "max": 30,
      "step": 0.5,
      "description": "Clip length. The button enters, then idles on a loop and stays on screen to the end."
    }
  ]
}
