{
  "id": "button-blink-flash",
  "name": "Button Blink Flash",
  "description": "Square button flashing two colours",
  "folder": "Buttons",
  "project": "src/projects/button-blink-flash.ts",
  "durationParam": "durationInSeconds",
  "defaultWidth": 1080,
  "defaultHeight": 1920,
  "params": [
    {
      "key": "text",
      "label": "Button text",
      "type": "text",
      "default": "Get Started",
      "description": "The label. The button sizes itself to it: longer text makes a wider button, very short text keeps a minimum width, and text too wide for the frame wraps onto more lines."
    },
    {
      "key": "backgroundColor",
      "label": "Button colour",
      "type": "color",
      "default": "#dc2626",
      "description": "Main fill colour."
    },
    {
      "key": "textColor",
      "label": "Text colour",
      "type": "color",
      "default": "#ffffff",
      "description": "Colour of the label."
    },
    {
      "key": "flashColor",
      "label": "Flash colour",
      "type": "color",
      "default": "#ea580c",
      "description": "The colour the fill switches to on each flash."
    },
    {
      "key": "blinkInterval",
      "label": "Blink interval (s)",
      "type": "number",
      "default": 0.5,
      "min": 0.2,
      "max": 4,
      "step": 0.05,
      "description": "Time between colour switches. Approximate: cycles are stretched or squeezed slightly so whole ones fill the clip and the button keeps moving to the last frame."
    },
    {
      "key": "fontSize",
      "label": "Font size (px)",
      "type": "number",
      "default": 64,
      "min": 16,
      "max": 200,
      "step": 1,
      "description": "Label size. The button's height follows it, and its width follows the measured text."
    },
    {
      "key": "positionY",
      "label": "Vertical position (0 top / 1 bottom)",
      "type": "number",
      "default": 0.5,
      "min": 0.05,
      "max": 0.95,
      "step": 0.01,
      "description": "Where the button's centre sits in the frame, 0 at the top edge and 1 at the bottom."
    },
    {
      "key": "durationInSeconds",
      "label": "Duration (s)",
      "type": "number",
      "default": 4,
      "min": 0.5,
      "max": 30,
      "step": 0.5,
      "description": "Clip length. The button enters, then idles on a loop and stays on screen to the end."
    }
  ]
}
